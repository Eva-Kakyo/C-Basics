#include <stdio.h>

//program to find the ASCII value of  a character.
int main () {
  char d;

  printf("Type a letter.\n");
  scanf("%c", &d);
  printf("The ASCII value of %c: %d", d, d);

  return 0;

}
